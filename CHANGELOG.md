# FITSOLO 迭代记录

本文件用于记录网站每一轮可交付迭代。代码只在用户验收并明确同意后提交和推送到远程仓库。

## [0.8.0] - 2026-09-05

### 真实打卡数据

- 个人中心训练概览新增真实打卡日历、累计打卡、连续打卡、月份切换和近期打卡时间轴。
- 新增“今日打卡”弹窗，可提交训练类型、训练时长和最多 500 字的训练备注。
- 提交成功后重新读取服务端记录，并自动刷新日历高亮、统计卡片和近期记录。
- 同一会员同一天再次提交会更新当天记录，避免重复累计。

### 后端与数据库

- 新增 D1 数据库绑定 `DB` 和 `check_in_records` 表迁移，数据在刷新页面或重新登录后继续保留。
- 新增 `POST /api/check-ins` 保存接口与 `GET /api/check-ins` 会员历史查询接口。
- 新增 `POST /api/auth/login` 服务端演示账号验证，密码不再进入前端 JavaScript。
- 会员接口使用 30 天有效的 HMAC 签名会话，后端从签名凭证解析用户 ID，不信任前端传入的用户 ID。
- 本地 Node 预览使用 SQLite 文件模拟正式 D1，方便在发布前完整验证持久化流程。

### 安全与约束

- 服务端限制训练类型、1–600 分钟训练时长和备注长度，并使用参数化 SQL 查询。
- 数据查询强制按当前会员 ID 过滤；无凭证、伪造凭证或过期凭证返回 401。
- 本轮完成数据库迁移、接口实现、自动化测试与正式发布准备。

## [0.7.0] - 2026-09-04

### 登录模块

- 登录页移除验证码输入框与获取验证码按钮，统一为“手机号 + 6 位密码”的会员登录流程。
- 新增 10 组一一对应的前端演示会员账号；验证成功直接进入个人中心，失败沿用原页面内错误提示样式。
- 游客登录调整为独立弱化按钮，无需填写表单即可直接进入网站主页。
- 会员和游客身份均通过浏览器 `localStorage` 保存，页面刷新与站内跳转后继续保持身份。
- 本地身份记录不包含演示密码，仅保存角色、脱敏展示所需手机号、会员编号与登录时间。

### 会员体验

- 新增个人中心页面骨架，集中展示会员信息、当前目标、方案周期、累计打卡和三个智能体快捷入口。
- 顶部导航根据身份动态展示：会员在“案例回放”之后显示“个人中心”，游客不显示。
- 个人中心增加会员访问守卫与退出登录入口，游客或未登录用户不能直接访问。
- 补齐导航菜单的移动端展开样式，保持新增菜单项在窄屏下可用。

### 说明

- 本轮为前端竞赛演示登录，不替代生产环境的服务端鉴权；正式上线需使用加密传输、密码哈希和服务端会话。
- 本轮已通过用户验收，并获准提交、推送与发布。

## [0.6.2] - 2026-09-04

### 修复

- GitHub Pages 地址现在自动转到正式托管站的对应页面，使智能体对话改为同源请求。
- 避免部分电脑的浏览器隐私策略、安全扩展或网络策略拦截跨站 SSE 对话流。
- 更新前端资源版本号，避免旧版脚本被浏览器缓存。

## [0.6.1] - 2026-09-04

### 修复

- 修复 GitHub Pages 国内备用地址无法调用智能体的问题，明确拆分静态前端与安全服务端代理。
- 备用站点自动把智能体请求发送到托管后端，DeepSeek API Key 继续只保存在服务端环境变量中。
- 新增跨域来源白名单与 `OPTIONS` 预检处理，仅允许 FITSOLO 正式站和 GitHub Pages 备用站调用接口。
- 新增 45 秒请求超时、网络异常与服务异常提示，避免对话界面长时间无反馈。

### 部署

- 新增 Cloudflare Workers 兼容的 DeepSeek SSE 流式代理，并纳入正式构建产物。
- 保留本地 Node.js 代理，同时补齐与线上一致的跨域安全策略。

## [0.6.0] - 2026-08-26

### 新增

- 新增贯穿全站 8 个页面的全局悬浮 FITSOLO Agent，以固定定位保持在浏览器可视区域内。
- 新增晶莹水滴入口，包含高光、背景透射、液态呼吸形变、Hover 晃动和双层水波纹反馈。
- 新增鼠标与触摸自由拖动、可视区域边界限制、位置记忆和窗口尺寸变化适配。
- 新增响应式聊天面板、消息列表、输入框、输入状态、键盘焦点与 Escape 关闭交互。
- 新增零依赖 Node.js 后端代理 `POST /api/agent/chat`，通过环境变量调用 DeepSeek Chat Completions。
- 新增 SSE 流式输出，前端聊天气泡随模型增量内容逐步更新。
- 新增 `.env.example` 安全配置模板；真实 `.env.local` 已由 `.gitignore` 排除。

### 调整

- 对话历史使用 `sessionStorage` 保存最近 30 条记录，在网站页面跳转后继续保留。
- 后端最多向模型发送最近 12 条对话，并附带当前页面标题与路径。
- 对话面板根据水滴位置自动调整展开方向；窄屏下固定在安全视口区域，避免溢出。
- 水滴配色降低主体不透明度并增强玻璃模糊、白色高光与折射质感。

### 安全与性能

- DeepSeek API Key 仅由 Node.js 后端读取 `DEEPSEEK_API_KEY`，不会进入 HTML、CSS、前端 JavaScript或版本库。
- System Prompt 配置为网站智能客服角色，回复风格要求热情、简短。
- 拖动阶段仅使用 `translate3d`，展开动画仅使用 `transform` 与 `opacity`，减少布局重排。
- 支持 `prefers-reduced-motion`，并使用 `textContent` 渲染消息以避免用户输入被解析为 HTML。

### 说明

- 本轮变更已合并到正式 `web/` 目录，并新增根目录 Node.js 服务端入口。
- 本轮已通过用户最终验收，并获准提交和推送到 GitHub。

## [0.5.0] - 2026-08-26

### 新增

- 在引导首屏新增原生 HTML5 Canvas 动态圆环背景，以多层细线扩散和轻微呼吸营造柔和科技感。
- 新增 Enter 按钮距离感应：鼠标越靠近按钮，圆环呼吸与扩散频率越快，并使用平滑插值避免速度突变。
- 在手机号验证码操作行新增“游客访问”入口，与“获取验证码”并排显示。
- 游客访问会记录当前访问模式并直接进入 `home.html`，不依赖登录接口或表单输入。

### 调整

- 移除引导页原有两个静态 CSS 同心圆伪元素，改为可自适应窗口尺寸和设备像素比的 Canvas 图层。
- 略微提高圆环基础呼吸频率，并维持原有深绿、低透明白色细线和荧光绿光晕视觉语言。
- 游客入口采用弱化的椭圆描边按钮，在桌面端与移动端保持合理间距，不占用登录表单底部空间。

### 性能与兼容

- 动画使用 `requestAnimationFrame` 驱动，设备像素比最高限制为 2，降低高分屏绘制开销。
- 页面隐藏、离开引导首屏时暂停动画，返回首屏后自动恢复。
- 窗口缩放使用逐帧节流；系统启用“减少动态效果”时展示静态圆环。

### 说明

- 本轮变更已经合并到正式 `web/` 目录并通过本地验证。
- 本轮已通过用户最终验收，并获准提交和推送到 GitHub。

## [0.4.1] - 2026-08-25

### 新增

- 新增 Sites 公网托管配置，为 FITSOLO 提供可从任意主流浏览器访问的生产网址。
- 新增零依赖生产构建脚本，将现有静态站点打包为 Cloudflare Workers 兼容产物。
- 新增社交分享预览资源配置，改善网站链接在聊天与社交平台中的展示效果。

### 说明

- 本轮仅增加发布与分享能力，不改变 `web/` 内已验收的产品交互。
- 正式生产地址：<https://fitsolo-ai-gcc1021.fuzzy-shrew-9655.chatgpt.site>。

## [0.4.0] - 2026-08-25

### 新增

- 在登录步骤新增“← 返回上一步”按钮，并将其与“登录并进入”按钮安排在同一操作行。
- 新增验证码登录模式，默认展示手机号、获取验证码、六位验证码和登录入口。
- 新增 60 秒验证码防刷倒计时，倒计时期间按钮不可重复触发。
- 新增账号密码登录模式，可与验证码登录在同一页面内平滑切换。
- 新增用户协议勾选校验；登录或第三方快捷登录前未勾选时会显示明确提示。
- 新增微信登录、QQ 登录的 OAuth 2.0 跳转配置与回调占位页面。
- 新增用户协议和隐私政策占位页面，供登录页合规链接访问。

### 调整

- 登录卡片重构为“会员体验说明 + 登录操作”双栏结构，并沿用深色、荧光绿、雪山和毛玻璃视觉语言。
- 引导页进入登录页、登录页返回引导页均改为无刷新 DOM 状态切换和双向过渡动画。
- 验证码/密码模式通过 `opacity` 与 `transform` 动画切换，避免布局跳动和页面闪烁。
- 所有主要按钮补充悬停、按下、禁用和键盘焦点反馈。

### 接口预留

- 短信验证码服务预留：`POST /api/auth/sms-code`。
- 微信授权入口预留：`/oauth2/authorize/wechat`。
- QQ 授权入口预留：`/oauth2/authorize/qq`。
- OAuth 回调页面：`/oauth-callback.html`。

### 说明

- 当前仍为静态前端演示；短信发送、密码认证和第三方 OAuth 需要后端服务与平台密钥后才能正式使用。
- 本轮尚未提交或推送到 GitHub，等待用户验收。

## [0.3.0] - 2026-08-25

### 新增

- 新增全屏 Landing Page，使用 FITSOLO 深色运动风视觉、品牌标题与胶囊形 Enter 按钮。
- 新增雪山背景的注册/登录体验，包含毛玻璃卡片、四项注册输入、登录模式和 QQ/微信入口。
- 新增 Landing Page 到注册界面的淡出上移动画，并兼容减少动态效果的系统设置。
- 新增移动端单列布局，表单区与操作区在窄屏下垂直排列。
- 新增 `web/home.html`，完整保留并承接原官网首页内容。
- 新增表单前端校验、注册/登录模式切换和无障碍焦点管理。

### 调整

- 将 `web/index.html` 调整为网站统一入口，访问顺序变为“引导页 → 注册/登录 → 官网主页”。
- 将全站导航的“首页”和品牌 Logo 链接更新为 `home.html`，确保进入旧站功能区后导航逻辑一致。
- 视觉配色沿用旧站深色背景与荧光绿强调色，并在注册页加入半透明白色毛玻璃层。

### 说明

- 当前注册、登录、QQ 和微信按钮为前端演示交互，未接入真实账号服务，也不会保存密码。
- 雪山背景当前使用 Unsplash 占位图链接，可在正式发布前替换为自有授权图片。
- 本轮已提交并推送到 GitHub，提交号为 `b7d3515`。
